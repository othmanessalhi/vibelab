import { RevealImageList } from "@/components/ui/reveal-images";

export default function About() {
  return (
    <section id="about" className="py-20 md:py-32 bg-background flex items-center justify-center">
       <RevealImageList />
    </section>
  );
}
