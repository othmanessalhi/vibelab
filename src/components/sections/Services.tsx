import { RevealImageList } from "@/components/ui/reveal-images";

export default function Services() {
  return (
    <section id="services" className="h-screen flex items-center justify-center bg-background">
      <RevealImageList />
    </section>
  );
}
